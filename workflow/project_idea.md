# Project outline

Goal: data analysis pipeline  
I want to build a data analysis pipeline where I load multiple parquet/csv data files, I merge them into a unique database, query the portion of data I need, apply a regression model and save the results into a storage that will be used for visualization. Data source is continously updated (every 15 minutes). Visulization can be executed on demand. Let us keep visualization aside for the moment.  
Please outline the work in a sequence of steps that you would follow. I suggest you some technologies with which I am familiar and evaluate if they can be useful:

- Python + pandas/polars for data processing
- scikit-learn
- mlflow
- DBT
- Prefect

Give me your idea and let us discuss it. Be professional: you are a Data Engineer.
